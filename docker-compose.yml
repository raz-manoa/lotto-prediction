services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: lotto
      POSTGRES_PASSWORD: lotto
      POSTGRES_DB: lotto
    ports:
      - "5433:5432"
    volumes:
      - lotto_pg_data:/var/lib/postgresql/data

volumes:
  lotto_pg_data:
